# Server origin dir
SERVER_DIR=root@tests_sshd_1:/usr

# Target dir to put all backup files
TARGET_DIR=/tmp

# Delete files older than X days
DELETE_OLDER_THAN_X_DAYS=5

# Compress subdirs
# 0 False; 1 True
COMPRESS_SUBDIR=1

# Set compression level
# 1 - low (fast)
# 9 - high (slow)
GZIP=-1
